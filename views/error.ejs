<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error | Typely</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #e0eafc, #cfdef3);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
      font-family: 'Poppins', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    main {
      display: flex;
      justify-content: center;
      align-items: center;
      flex: 1;
      padding: 20px;
    }

    .error-container {
      background: #ffffff;
      padding: 50px 40px;
      border-radius: 16px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 480px;
      width: 100%;
      animation: fadeIn 0.7s ease-in-out;
      transition: all 0.3s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .error-icon {
      font-size: 55px;
      color: #ffb400; /* soft warning yellow */
      margin-bottom: 15px;
    }

    h1 {
      font-size: 2.2rem;
      font-weight: 700;
      color: #3b1c8c;
      margin-bottom: 10px;
    }

    p {
      color: #6c757d;
      font-size: 1.05rem;
      line-height: 1.6;
      margin-bottom: 35px;
    }

    a.return-home {
      display: inline-block;
      background: linear-gradient(to right, #3b1c8c, #6f42c1);
      color: #fff;
      text-decoration: none;
      padding: 12px 32px;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(59, 28, 140, 0.3);
    }

    a.return-home:hover {
      background: linear-gradient(to right, #2c0f77, #5e30a1);
      transform: translateY(-2px);
      box-shadow: 0 6px 18px rgba(59, 28, 140, 0.4);
    }

    footer {
      background: #ffffff;
      border-top: 1px solid rgba(0, 0, 0, 0.05);
      padding: 15px 0;
      box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.05);
    }

    footer .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 960px;
      margin: 0 auto;
      padding: 0 20px;
      flex-wrap: wrap;
    }

    .footer-logo {
      background: linear-gradient(to right, #3b1c8c, #6f42c1);
      color: #fff;
      text-decoration: none;
      padding: 8px 18px;
      border-radius: 8px;
      font-weight: 600;
      transition: all 0.3s ease;
      font-size: 1rem;
    }

    .footer-logo:hover {
      background: linear-gradient(to right, #2a0d6a, #5c2fa3);
      transform: translateY(-2px);
    }

    footer span {
      color: #6c757d;
      font-size: 0.9rem;
    }

    @media (max-width: 576px) {
      .error-container {
        padding: 35px 25px;
      }
      h1 {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>

  <main>
    <div class="error-container">
      <div class="error-icon">⚠️</div>
      <h1>Oops! Something went wrong</h1>
      <p><%= message || 'We encountered an unexpected issue. Please try again later.' %></p>
      <a href="/" class="return-home">Return Home</a>
    </div>
  </main>



</body>
</html>
