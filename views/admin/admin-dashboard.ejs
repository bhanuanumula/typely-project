<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Admin Dashboard | Typely</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
<style>
/* ---------------- General ---------------- */
body {
    font-family: 'Inter', sans-serif;
    background: #eef2ff;
    margin: 0;
    min-height: 100vh;
}

/* ---------------- Header ---------------- */
.admin-header {
    background: linear-gradient(90deg, #1e3c72, #2a5298);
    color: white;
    padding: 20px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-radius: 0 0 20px 20px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}
.admin-header h2 {
    font-size: 2rem;
    font-weight: 700;
    margin: 0;
}
.admin-header .user-info {
    display: flex;
    align-items: center;
    gap: 20px;
    font-size: 0.95rem;
}
.logout-btn {
    background-color: #fff;
    color: #1e3c72;
    font-weight: 600;
    border-radius: 8px;
    padding: 8px 18px;
    text-decoration: none;
    transition: all 0.3s ease;
}
.logout-btn:hover {
    background-color: #f0f0ff;
    transform: scale(1.08);
}

/* ---------------- Stats Cards ---------------- */
.stats-card {
    background: linear-gradient(145deg, #ffffff, #f0f2ff);
    border-radius: 20px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.08);
    padding: 30px;
    text-align: center;
    transition: all 0.4s ease;
    position: relative;
}
.stats-card:hover {
    transform: translateY(-7px);
    box-shadow: 0 15px 35px rgba(0,0,0,0.12);
}
.stats-card h3 {
    font-size: 2.2rem;
    color: #1e3c72;
    font-weight: 700;
}
.stats-card p {
    margin: 0;
    font-size: 1.1rem;
    color: #555;
}

/* Badge for pending items */
.badge {
    position: absolute;
    top: 15px;
    right: 15px;
    background-color: #ffc107;
    color: #000;
    font-weight: 600;
    padding: 5px 10px;
    border-radius: 12px;
    font-size: 0.8rem;
}

/* ---------------- Navigation Cards ---------------- */
.nav-card {
    background: white;
    border-radius: 20px;
    box-shadow: 0 6px 25px rgba(0,0,0,0.08);
    padding: 25px 30px;
    cursor: pointer;
    transition: all 0.4s ease;
    position: relative;
}
.nav-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 35px rgba(0,0,0,0.15);
    background: #f5f7ff;
}
.nav-card h4 {
    color: #1e3c72;
    font-weight: 700;
    margin-bottom: 10px;
    font-size: 1.25rem;
}
.nav-card p {
    color: #666;
    font-size: 0.95rem;
}

/* Tooltip styles */
.nav-card[data-tooltip]:hover::after {
    content: attr(data-tooltip);
    position: absolute;
    bottom: -30px;
    left: 20px;
    background: #1e3c72;
    color: #fff;
    padding: 5px 10px;
    border-radius: 6px;
    font-size: 0.8rem;
    white-space: nowrap;
}

/* ---------------- Footer ---------------- */
.footer {
    text-align: center;
    color: #888;
    margin-top: 50px;
    font-size: 0.9rem;
}

/* ---------------- Responsive ---------------- */
@media (max-width: 768px) {
    .stats-card h3 { font-size: 1.8rem; }
    .nav-card h4 { font-size: 1.1rem; }
    .admin-header { flex-direction: column; align-items: flex-start; gap: 10px; }
}
</style>
</head>
<body>

<div class="admin-header">
    <h2>Typely Admin Dashboard</h2>
    <div class="user-info">
        <span>Welcome, <strong><%= user.username %></strong> 👋</span>
        <a href="/admin/logout" class="logout-btn">Logout</a>
    </div>
</div>

<div class="container mt-5 mb-5">
    <!-- Stats Overview -->
    <div class="row g-4 mb-5">
        <div class="col-md-3">
            <div class="stats-card">
                <h3><%= users.length %></h3>
                <p>Total Users</p>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stats-card">
                <h3><%= blogs.length %></h3>
                <p>Total Blogs</p>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stats-card">
                <h3><%= users.filter(u => u.role==='admin').length %></h3>
                <p>Admins</p>
            </div>
        </div>
        <div class="col-md-3">
            <div class="stats-card">
                <h3><%= blogs.filter(b => b.status==='pending').length %></h3>
                <p>Pending Blogs</p>
                <% if(blogs.filter(b => b.status==='pending').length > 0){ %>
                <span class="badge">New</span>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Navigation Cards -->
    <div class="row g-4">
        <div class="col-md-6">
            <div class="nav-card" onclick="location.href='/admin/users'" data-tooltip="Manage all registered users">
                <h4>👥 Manage Users</h4>
                <p>Promote, demote, delete or reset user passwords</p>
            </div>
        </div>
        <div class="col-md-6">
            <div class="nav-card" onclick="location.href='/admin/blogs'" data-tooltip="Approve, edit, or delete blogs">
                <h4>📝 Manage Blogs</h4>
                <p>Edit, approve, delete blogs or filter by status</p>
            </div>
        </div>
        <div class="col-md-6">
            <div class="nav-card" onclick="location.href='/admin/contact-messages'" data-tooltip="View all messages submitted via contact form">
                <h4>📨 Contact Messages</h4>
                <p>View all messages sent via contact form</p>
            </div>
        </div>
    </div>

    <p class="footer">© 2025 Typely Admin Dashboard</p>
</div>

</body>
</html>
