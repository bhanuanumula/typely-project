<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Admin Messages | Typely</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
<style>
/* ---------------- General ---------------- */
body {
    font-family: 'Inter', sans-serif;
    background: #eef2ff;
    margin: 0;
    min-height: 100vh;
}

/* ---------------- Header ---------------- */
.admin-header {
    background: linear-gradient(90deg, #1e3c72, #2a5298);
    color: white;
    padding: 20px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-radius: 0 0 20px 20px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}
.admin-header h2 {
    font-size: 2rem;
    font-weight: 700;
    margin: 0;
}
.admin-header .user-info {
    display: flex;
    align-items: center;
    gap: 20px;
    font-size: 0.95rem;
}
.logout-btn, .back-btn {
    background-color: #fff;
    color: #1e3c72;
    font-weight: 600;
    border-radius: 8px;
    padding: 8px 18px;
    text-decoration: none;
    transition: all 0.3s ease;
}
.logout-btn:hover, .back-btn:hover {
    background-color: #f0f0ff;
    transform: scale(1.08);
}

/* ---------------- Message Cards ---------------- */
.message-card {
    background: #ffffff;
    border-radius: 12px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.12);
    margin-bottom: 20px;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.message-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 35px rgba(0,0,0,0.18);
}
.message-card-header {
    background: linear-gradient(135deg, #1e3c72, #2a5298);
    color: #fff;
    padding: 12px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.message-card-header h5 {
    margin: 0;
    font-weight: 600;
}
.message-card-body {
    padding: 20px;
}
.message-card-body p {
    margin: 6px 0;
    color: #555;
}
.message-card-body .email {
    font-style: italic;
    color: #333;
}
.message-card-body .timestamp {
    font-size: 0.85rem;
    color: #999;
    display: inline-block;
    margin-top: 10px;
    background-color: #f0f2ff;
    padding: 4px 10px;
    border-radius: 12px;
}

/* ---------------- Footer ---------------- */
.footer {
    text-align: center;
    color: #888;
    margin-top: 50px;
    font-size: 0.9rem;
}

/* ---------------- Responsive ---------------- */
@media (max-width: 768px) {
    .admin-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
    }
}
</style>
</head>
<body>

<!-- Admin Header -->
<div class="admin-header">
    <div class="d-flex align-items-center gap-3">
        <a href="/admin" class="back-btn">← Back</a>
        <h2 class="mb-0">Admin Messages</h2>
    </div>
    <div class="user-info">
        <span>Welcome, <strong><%= user.username %></strong> 👋</span>
        <a href="/admin/logout" class="logout-btn">Logout</a>
    </div>
</div>

<div class="container mt-4">

  <% if(messages.length === 0) { %>
    <p class="text-center text-muted mt-5">No messages submitted yet.</p>
  <% } else { %>
    <% messages.forEach(msg => { %>
    <div class="message-card">
        <div class="message-card-header">
            <h5><%= msg.name %></h5>
            <span class="timestamp"><%= new Date(msg.created_at).toLocaleString('en-US') %></span>
        </div>
        <div class="message-card-body">
            <p class="email">📧 <%= msg.email %></p>
            <p>💬 <%= msg.message %></p>
        </div>
    </div>
    <% }) %>
  <% } %>

</div>

<!-- Admin Footer -->
<p class="footer">© 2025 Typely Admin Dashboard</p>

</body>
</html>
